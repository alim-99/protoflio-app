/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
      domains: ['localhost', 'https://protoflio-app.vercel.app/'],
    },
};

export default nextConfig;
